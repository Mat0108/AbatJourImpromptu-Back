
services:
  serverabatjour:
    container_name: serverabatjour
    # depends_on:
    #   - mongodb
    #on build le server avec le dockerfile du dossier server
    build: ./
    restart: always
    env_file: .env
    ports:
      - 8082:8082
      - 466:466
    environment:
        - ENV_TYPE=prod
        - NODE_PORT= 8082
        - MONGODB_URL=mongodb://admin:F8G3iKGT%408cASsGy@37.59.112.87:27017/AbatJourImpromptu?authSource=admin&authMechanism=SCRAM-SHA-256

    stdin_open: true
    tty: true
    cap_drop:
      - ALL
    networks:
      - web

networks:
  web:
      external: true
